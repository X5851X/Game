{
  "name": "truth-and-lie-frontend",
  "version": "1.0.0",
  "description": "Frontend for Truth and Lie Game",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=3000",
    "build": "mkdir -p dist && cp -r *.html *.css *.js dist/"
  },
  "dependencies": {
    "socket.io-client": "^4.7.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}